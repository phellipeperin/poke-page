module.exports = {
    theme: {
        textColor: (theme) => ({
            ...theme('colors'),
            primary: '#DB2777',
            secondary: '#ffed4a',
        }),
        backgroundColor: (theme) => ({
            ...theme('colors'),
            primary: '#DB2777',
            secondary: '#ffed4a',
        }),
    },
};
